@using AppBuilder.Models
@inject NavigationManager Nav

<MudPaper Outlined="true" Class="card">
  @if (!string.IsNullOrWhiteSpace(Item.CoverImage))
  {
    <MudImage Src="@Item.CoverImage" Height="140" ObjectFit="ObjectFit.Cover" />
  }

  <MudStack Class="p-4" Spacing="1">
    <MudText Typo="Typo.h6">@Item.Title</MudText>
    <MudText Typo="Typo.body2" Color="Color.Secondary">@Item.Summary</MudText>

        <MudStack Row="true"
                  Spacing="1"
                  Class="mt-2"
                  Wrap="Wrap.Wrap">
      @foreach (var t in Item.Tags.Take(4))
      {
                <MudChip T="string"
                         Size="Size.Small"
                         Variant="Variant.Filled"
                         Color="Color.Primary">
                    @t
                </MudChip>
      }
    </MudStack>

    <MudStack Row="true" Spacing="2" Class="mt-3">
      @if (!string.IsNullOrWhiteSpace(Item.DownloadUrl))
      {
        <MudButton Variant="Variant.Outlined" Size="Size.Small" Href="@Item.DownloadUrl" Target="_blank">Descargar</MudButton>
      }
      <MudButton Variant="Variant.Filled" Color="Color.Primary" Size="Size.Small"
                 OnClick="@(() => Nav.NavigateTo($"/resources/{Item.Slug}"))">
        Ver más
      </MudButton>
    </MudStack>
  </MudStack>
</MudPaper>

@code {
  [Parameter, EditorRequired] public ResourceItem Item { get; set; } = default!;
}